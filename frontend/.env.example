# Frontend Environment Variables
# Copy this file to .env.local (local) or configure in Render Static Site settings.

# Preferred (production): full backend base URL
VITE_API_BASE_URL=https://ck-ptud-backend.onrender.com

# Optional fallback for local/LAN development
VITE_SERVER_IP=localhost
VITE_SERVER_PORT=8000
